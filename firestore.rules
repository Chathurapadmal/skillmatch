rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rule: any signed-in user can read/write documents.
    // Tighten this before production deployment.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
